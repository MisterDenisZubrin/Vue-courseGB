import { mount } from '@vue/test-utils';
import Form from '../components/Form.vue';

describe('Form tests', () => {
  test('Category test', () => {
    const wrapper = mount(Form);
    debugger
  });
});